# Example environment variables for ollama-guard
# WARNING: Do NOT commit real secrets to version control. Use a secrets manager for production.

# App runtime
APP_ENV=development
APP_DEBUG=1

# Upstream Ollama server (no trailing slash recommended)
OLLAMA_UPSTREAM=https://ollama.example.com

# Authentication
# Either set a static API key or configure JWT settings below.
PROXY_API_KEY=your-secret-key

# JWT (HS256) secret used to verify Bearer tokens
PROXY_JWT_SECRET=SuperSecretKey
# Optional: if set, the JWT audience will be validated
PROXY_JWT_AUD=ollama
# Optional: if set, the JWT issuer (iss claim) will be validated
PROXY_JWT_ISS=https://your-issuer.example.com

# Server bind options (used if running with uvicorn directly)
HOST=0.0.0.0
PORT=8000
